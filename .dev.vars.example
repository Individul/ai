# Copiaza in .dev.vars (niciodata comis). Identitatea folosita local, in lipsa Cloudflare Access.
DEV_EMAIL=dev@local
# Cine poate intra la /admin (lista separata prin virgula). In productie se pune in wrangler.jsonc, vars.ADMIN_EMAILS.
ADMIN_EMAILS=dev@local
# Cheia Gemini API (Google AI Studio, proiect cu facturare). Fara ea, chatul si indexarea raspund 503.
GEMINI_API_KEY=
# Cheia Z.AI (consola z.ai, planul de coding). Fara ea, chatul pe modelele GLM raspunde 503.
ZAI_API_KEY=
# Optional: alt endpoint OpenAI-compatibil (implicit https://api.z.ai/api/coding/paas/v4, al planului de coding).
# ZAI_API_BASE=
